// Route constants
const HOME_ROUTE = '/';

// Environment constants
const CONNECTED_URI = process.env.CONNECTED_URI;
const PORT = process.env.PORT || 3000;

module.exports = {
    HOME_ROUTE,
    CONNECTED_URI,
    PORT
}